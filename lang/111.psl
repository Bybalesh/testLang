Prism.languages.pcaxis = {
	'string': /"[^"]*"/,
	'keyword': {
	program HelloWorld(input, output);
```psl
-- PSL Properties for the system

default clock is rising_edge(clk);

property req_ack_property:
    always (req -> next ack);

assert req_ack_property;
```
