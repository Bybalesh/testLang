Prism.languages.rip = {
	'comment': {
		pattern: /#.*/,
		greedy: true
	},
program Counter;

var
  Count: Integer;
  
begin
  Count := 0;
  repeat
    Inc(Count);
    WriteLn('Current count:', Count);
    Sleep(1000);
  until Count >= 10;
end.
